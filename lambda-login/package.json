{
  "name": "jwt-login-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
		"test": "npm run test:unit && npm run test:integration",
		"test:unit": "mocha test/unit",
		"test:integration": "mocha test/integration",
		"test_ci": "npm run test_ci:unit && npm run test_ci:integration",
		"test_ci:unit": "mocha test/unit --reporter mocha-junit-reporter --reporter-options mochaFile=./junit.xml",
		"test_ci:integration": "mocha test/integration --reporter mocha-junit-reporter --reporter-options mochaFile=./junit.xml"
  },
	"author": "Nikxy",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.382.0",
    "@aws-sdk/client-secrets-manager": "^3.379.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
		"mocha": "^10.2.0",
		"mocha-junit-reporter": "^2.2.1"
  }
}
